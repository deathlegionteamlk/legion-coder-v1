import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.legion.coder',
  appName: 'LEGION Coder v1',
  webDir: 'dist/renderer'
};

export default config;
